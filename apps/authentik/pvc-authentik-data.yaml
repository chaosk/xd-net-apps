# File storage for Authentik (AUTHENTIK_STORAGE__FILE__PATH default /data).
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik-data
  namespace: authentik
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: synology
  resources:
    requests:
      storage: 1Gi
