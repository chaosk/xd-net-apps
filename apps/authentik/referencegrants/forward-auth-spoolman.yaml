apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: forward-auth-spoolman
  namespace: authentik
spec:
  from:
    - group: gateway.envoyproxy.io
      kind: SecurityPolicy
      namespace: spoolman
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: spoolman
  to:
    - group: ""
      kind: Service
      name: authentik-server
