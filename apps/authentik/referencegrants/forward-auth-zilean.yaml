apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: forward-auth-zilean
  namespace: authentik
spec:
  from:
    - group: gateway.envoyproxy.io
      kind: SecurityPolicy
      namespace: zilean
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: zilean
  to:
    - group: ""
      kind: Service
      name: authentik-server
