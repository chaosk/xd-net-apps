apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: forward-auth-peanut
  namespace: authentik
spec:
  from:
    - group: gateway.envoyproxy.io
      kind: SecurityPolicy
      namespace: peanut
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: peanut
  to:
    - group: ""
      kind: Service
      name: authentik-server
