apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: forward-auth-invidious
  namespace: authentik
spec:
  from:
    - group: gateway.envoyproxy.io
      kind: SecurityPolicy
      namespace: invidious
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: invidious
  to:
    - group: ""
      kind: Service
      name: authentik-server
