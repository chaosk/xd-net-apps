apiVersion: v1
kind: Service
metadata:
  name: tubearchivist
  namespace: tubearchivist
spec:
  selector:
    app: tubearchivist
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP
