apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spoolman-data
  namespace: spoolman
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: synology
  resources:
    requests:
      storage: 2Gi
