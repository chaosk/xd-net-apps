apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - namespace.yaml
  - postgres.yaml
  - httproute.yaml
  - scrape-cnpg.yaml
  - scrape-apps.yaml
  - scrape-platform.yaml
  - dashboards/

# kube-prometheus-stack renders hostNetwork: false explicitly; the API/operator often
# omits false defaults on read. Strip them so Git matches live state (Argo CD diff).
patchesJson6902:
  - target:
      group: monitoring.coreos.com
      kind: Alertmanager
      name: kube-prometheus-stack-alertmanager
    patch: |
      - op: remove
        path: /spec/hostNetwork
  - target:
      group: monitoring.coreos.com
      kind: Prometheus
      name: kube-prometheus-stack-prometheus
    patch: |
      - op: remove
        path: /spec/hostNetwork
  - target:
      group: apps
      kind: Deployment
      name: kube-prometheus-stack-kube-state-metrics
    patch: |
      - op: remove
        path: /spec/template/spec/hostNetwork

helmCharts:
  - name: kube-prometheus-stack
    repo: https://prometheus-community.github.io/helm-charts
    version: 85.3.0
    releaseName: kube-prometheus-stack
    namespace: monitoring
    valuesFile: values-prometheus.yaml
  - name: loki
    repo: https://grafana.github.io/helm-charts
    version: 7.0.0
    releaseName: loki
    namespace: monitoring
    valuesFile: values-loki.yaml
  - name: alloy
    repo: https://grafana.github.io/helm-charts
    version: 1.8.1
    releaseName: alloy
    namespace: monitoring
    valuesFile: values-alloy.yaml
