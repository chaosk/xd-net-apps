apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: homepage

resources:
  - namespace.yaml
  - rbac.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml

images:
  - name: ghcr.io/gethomepage/homepage
    newTag: v1.13.0
