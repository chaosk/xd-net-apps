# Examples for image-updater.yaml — copy an applicationRefs block into that file.
#
# bjw-s app-template Helm (default write-back: helmvalues:values.yaml):
#
#     - namePattern: my-app
#       images:
#         - alias: my-app
#           imageName: ghcr.io/example/my-app:~1.2
#           manifestTargets:
#             helm:
#               name: controllers.main.containers.main.image.repository
#               tag: controllers.main.containers.main.image.tag
#
# Kustomize (override write-back target):
#
#     - namePattern: my-kustomize-app
#       writeBackConfig:
#         method: git
#         gitConfig:
#           writeBackTarget: kustomization
#       images:
#         - alias: my-kustomize-app
#           imageName: ghcr.io/example/app:~1.0
#           manifestTargets:
#             kustomize:
#               name: ghcr.io/example/app
#
# Semver + tag filters on one image:
#
#         - alias: app
#           imageName: ghcr.io/example/app:~v3
#           commonUpdateSettings:
#             updateStrategy: semver
#             allowTags: "regexp:^v3\\.[0-9]+\\.[0-9]+$"
#             ignoreTags: ["*-rc*", "*-beta*", "latest"]
